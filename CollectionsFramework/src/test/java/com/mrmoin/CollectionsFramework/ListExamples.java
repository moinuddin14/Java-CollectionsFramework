package com.mrmoin.CollectionsFramework;

public class ListExamples {

	/**
	 * List (I): List is an Interface. List is the child Interface of
	 * Collection(I). If we want to represent a group of individual objects as a
	 * single entity where duplicates are allowed and insertion order must be
	 * preserved then we should go for List(I).
	 * 
	 */

}
